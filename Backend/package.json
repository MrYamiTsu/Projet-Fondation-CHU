{
  "name": "express-starter",
  "version": "1.0.0",
  "private": true,
  "description": "Base de projet Express.js en JavaScript avec modules ES.",
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env src/server.js",
    "start": "node --env-file-if-exists=.env src/server.js"
  },
  "engines": {
    "node": ">=22.9.0"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
